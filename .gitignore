# Xcode
xcuserdata/
*.xcuserstate
*.xcscmblueprint
DerivedData/
.build/

# Swift Package Manager
.swiftpm/
Package.resolved

# CocoaPods (only if you ever add it)
Pods/

# Secrets / environment-specific config
Configs/Secrets.xcconfig

# Google Sign-In config (if you ever re-add a standalone client plist)
GoogleService-Info.plist
client_*.googleusercontent.com.plist

# macOS
.DS_Store

# Simulator/device logs (optional, if they ever clutter the repo)
*.ips
